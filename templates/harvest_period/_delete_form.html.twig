<form method="post" action="{{ path('harvest_period_delete', {'id': harvest_period.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ harvest_period.id) }}">
    <button class="btn">Delete</button>
</form>
