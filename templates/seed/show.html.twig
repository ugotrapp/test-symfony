{% extends 'base.html.twig' %}

{% block title %}Seed{% endblock %}

{% block body %}
    <h1>Seed</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seed.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ seed.name }}</td>
            </tr>
            <tr>
                <th>Advice</th>
                <td>{{ seed.advice }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ seed.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('seed_index') }}">back to list</a>

    <a href="{{ path('seed_edit', {'id': seed.id}) }}">edit</a>

    {{ include('seed/_delete_form.html.twig') }}
{% endblock %}
