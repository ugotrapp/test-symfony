<form method="post" action="{{ path('planting_period_delete', {'id': planting_period.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planting_period.id) }}">
    <button class="btn">Delete</button>
</form>
